{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core APIs plus the native save/open dialogs. Reading and writing project files is not here: it goes through this app's own `project_*` commands, which are not permission-gated, so no filesystem scope has to be granted.",
  "windows": ["*"],
  "permissions": ["core:default", "dialog:allow-save", "dialog:allow-open"]
}
